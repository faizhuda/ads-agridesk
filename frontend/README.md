# Agridesk Frontend (React + Vite)

## Setup

```bash
npm install
npm run dev
```

Frontend runs on `http://localhost:5173` and proxies `/api/*` to backend `http://localhost:8000`.

## Optional

Set custom backend URL with env var:

```bash
VITE_API_BASE=http://localhost:8000
```